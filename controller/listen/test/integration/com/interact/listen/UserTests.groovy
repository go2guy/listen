package com.interact.listen

class UserTests extends GroovyTestCase {
/*    void testLookupRealNameFromAni0() {
        final String ani = '14024768786'
        final String expected = 'Theodore Roosevelt'

        def organization = new Organization(name: 'Example Organization').save()
        assert organization != null

        def user = new User(emailAddress: 'foo@example.com',
                            organization: organization,
                            password: '0',
                            realName: expected,
                            username: 'teddy').save()
        assert user != null

        def phone = new Phone(greetingLocation: '/tmp/example',
                              number: ani,
                              owner: user).save()

        assert phone != null

        assertEquals expected, User.lookupRealNameByAni(ani)
    }

    // no ani found, expecting null
    void testLookupRealNameFromAni1() {
        assertNull User.lookupRealNameByAni('14024768786')
    }*/
}
